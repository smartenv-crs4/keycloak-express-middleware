{
  "name": "keycloak-express-middleware-test",
  "version": "1.0.0",
  "description": "Test suite for keycloak-express-middleware",
  "private": true,
  "scripts": {
    "setup-keycloak": "NODE_ENV=test PROPERTIES_PATH=./config node helpers/keycloak-setup.js",
    "test": "NODE_PATH=./node_modules NODE_ENV=test PROPERTIES_PATH=./config mocha --require support/setup.js --exit"
  },
  "dependencies": {
    "@keycloak/keycloak-admin-client": "^26.5.5",
    "chai": "^4.3.7",
    "express": "^4.18.2",
    "express-session": "^1.18.1",
    "keycloak-connect": "^26.1.1",
    "keycloak-express-middleware": "file:..",
    "node-fetch": "^2.6.7",
    "propertiesmanager": "^4.1.0",
    "supertest": "^7.2.2"
  },
  "devDependencies": {
    "mocha": "^11.7.5"
  }
}
